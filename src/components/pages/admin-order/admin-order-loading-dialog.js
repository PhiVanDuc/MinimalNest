export default function AdminOrderLoadingDialog() {
    return (
        <div className="fixed inset-0 flex items-center justify-center bg-black/80 z-[50]">
            <p className="text-[16px] text-white">Đang tải . . .</p>
        </div>
    )
}
